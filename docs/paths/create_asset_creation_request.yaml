/createAssetCreationRequest:
  get:
    tags:
      - Assets
    summary: Create `CREATE_ASSET` request
    description: |
                  Allows to create or update `CREATE_ASSET` reviewable request
                  #### Account Role Requirements
                    | Account | Resource Type | Rule Action | Details |
                    | --------|:-------------:| -----------:| ------------ |
                    | source  | `ASSET`       | `CREATE`    | `CREATE_WITH_TASKS` rule action required if `allTasks` not null. Resource include code and type of asset |

                  #### Signer Role Requirements
                    | Resource Type | Rule Action | Details |
                    |:-------------:| -----------:| --------|
                    | `ASSET`       | `CREATE`    | Resource includes code and type of asset |
    operationId: manageAssetOp
    parameters:
    - name: Manage Asset Op
      in: query
      required: false
      style: form
      explode: true
      schema:
        type: object
        $ref: '#/components/schemas/ManageAssetOp'
    responses:
      200:
        description: Result
        content:
          application/vnd.api+json:
            schema:
              type: object
              $ref: '#/components/schemas/ManageAssetResult'
