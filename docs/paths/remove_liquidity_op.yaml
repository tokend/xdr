/removeLiquidity:
  get:
    tags:
      - "Liquidity Pool"
    summary: Remove liquidity from the pool
    description: |
      Allow to remove liquidity from the pool
    operationId: lpRemoveLiquidityOp
    x-item-type: operation
    parameters:
    - name: LPRemoveLiquidityOp
      in: query
      required: false
      style: form
      explode: true
      schema:
        type: object
        $ref: '#/components/schemas/LpRemoveLiquidityOp'
    responses:
      200:
        description: Result
        content:
          xdr:
            schema:
              type: object
              $ref: '#/components/schemas/LpRemoveLiquidityResult'
